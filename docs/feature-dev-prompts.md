# 功能开发提示词（Prompts）

> 用途：后续真正落地开发时，把对应章节的完整内容贴给 AI 作为需求说明。
> 每份提示词均包含：项目背景、需求、已选定的技术路线、约束、验收标准。
> 请勿在开发时偏离"已选定的技术路线"，如需变更先与我确认。

---

## 开发顺序与依赖（开工前先读这一节）

### 最终目标

两套并行的工作方式，共用同一套对比内核（`src/compute.js`）：

- **少量文件** → 用现有的 UI 页面直观看差异（**已实现，不需要开发**）
- **文件多 / 不想看 UI** → 通过和 deepseek harness（dsh）对话描述怎么对比，由应用生成**自包含 HTML 对比报告**供人查看

### 提示词之间的关系

| 提示词                         | 状态                         | 说明                                                                                                                                  |
| ------------------------------ | ---------------------------- | ------------------------------------------------------------------------------------------------------------------------------------- |
| 提示词 1（PaddleOCR）          | **独立，可选**         | 与主线无依赖，随时可做，也可暂缓                                                                                                      |
| 提示词 2（云端大模型语义识别） | ⛔**已作废，不要实施** | 接上 dsh 后整块由 harness 承担（dsh 自带 LLM + 工具调用）。应用侧**不再**需要 API Key 配置界面、厂商兼容、prompt 编排、结果渲染 |
| 提示词 3（对比结果导出）       | 部分并入提示词 4             | 其中的**报告模板 `src/report.js` 与提示词 4 共用**；"人在页面里点导出"这一入口可延后                                          |
| 提示词 4（无头渲染通道）       | **主线核心**           | 一切的承重墙，先做                                                                                                                    |
| 提示词 5（dsh 插件对接）       | 最后做                       | 依赖提示词 4 的`tools/render.js` 已稳定                                                                                             |

### 推荐开发顺序

按阶段推进，**每个阶段结束都应是一个可验证的完整状态**，不要跨阶段并行。

#### 阶段 A：前置重构（无用户可见变化）

- **A1. 抽出 `src/pipeline.js`**（提示词 4 第 1 项）
  把 `src/app.js`（1539 行）中"加载一对文件 → 计算差异 → 把差异映射到结果区与 PDF/docx 面板并上色"的逻辑抽成独立模块，不依赖标签页/历史/UI 事件。
  **这是所有后续工作的前置依赖**：不抽，无头驱动就得复制一份上色逻辑，两边必然走样。
  验收：`npm test` / `npm run test:all` 全绿，界面行为完全不变。

#### 阶段 B：最小可用闭环（一条命令出一份单对报告）

- **B1. 补验 docx 逐页截图**（半天，可先做）
  当前**唯一没实证的环节**。扩展现有的 `tools/spike-capture.js`：换 `.docx` 夹具 + 走 `DocxView.load`，验证 docx 面板的"页"（docx-preview `breakPages` 生成的块）能逐页截到且不漏页。
  **这一步没过就不要往下做**，否则报告模板要返工。
- **B2. `src/report.js` 报告模板**（提示词 4 第 5 项）
  先**只做左右分栏视图**，跳过 Redline 与批量总览，够跑通闭环即可。
  自包含单文件、CSS/JS/图片全内联、接收方零安装双击即看。
- **B3. `tools/render.js` 无头任务入口**（提示词 4 第 2、3 项）
  起临时静态服务 + headless Edge + CDP 驱动页面。**从 `tools/spike-capture.js` 起步**，它已有 CDP 连接、超时、看门狗、逐页滚动收敛等基础设施，不要从零重写。
- **B4. 逐页快照采集接入**（提示词 4 第 4 项）
  照抄 spike 里已验证的做法，并逐条落实"已知的坑"一节的 6 条措施。
- **阶段 B 完成标志**：`node tools/render.js --task demo.json` 在无人工介入下产出一份单对报告，在**未安装本应用**的电脑上双击打开，4 区块完整、颜色标注与页面所见一致、快照放大后清晰。

#### 阶段 C：批量与双视图（提示词 3 的 ②③）

- **C1.** 批量总览（每对一行：文件名、差异数、锚点超链接）
- **C2.** Redline 合并视图（红删除线 + 蓝下划线），与分栏视图报告内一键切换
- **C3.** 分卷：**一份报告最多 10 对**，超出由调用方切成多份

#### 阶段 D：对接 dsh（提示词 5）

- 插件壳（自定义命令 / skill）+ 任务 JSON schema 与范例 + 自然语言→任务 JSON 的提示词
- 依赖：阶段 B/C 的 `tools/render.js` 已稳定

#### 阶段 E：可选 / 独立

- **E1. 提示词 1**：扫描版 PDF 的 PaddleOCR（与主线无依赖）
- **E2. 桌面（Tauri）模式的无头渲染**（提示词 4 文末"后续扩展"）：两条路，优先考虑让 WebView2 开放调试端口以复用同一套 CDP 驱动代码

### 交给 AI 开发时的注意事项

1. **一次只做一个阶段**，把该阶段对应的提示词章节 + 本节一起给它。
2. **明确告知它提示词 2 已作废**，避免它去实现云端大模型 API 配置那一整块。
3. **让它先读 `tools/spike-capture.js` 的文件头**——验证结论、实测数据、5 条坑及其对应代码位置都写在那里。
4. 每个阶段结束时要求它跑 `npm test` / `npm run test:all`，确保既有功能不回归。

---

## 提示词 1：扫描版 PDF 的 OCR 识别（连接本机 PaddleOCR 环境）（或者deepseek harness社区插件调用）

### 项目背景

这是一个 Tauri 2 + 纯前端（无框架，原生 JS）的离线文本对比工具。仓库结构：

- 前端入口 `index.html`，逻辑在 `src/` 下按模块拆分（`app.js`、`pdfview.js`、`compute.js` 等）
- PDF 渲染与文本提取用 pdf.js（`lib/pdf.min.js`），文本提取在 `src/pdfview.js` 的 `extractText`（基于 `getTextContent`，**只能提取文字版 PDF，扫描件提取结果为空**）
- Rust 后端在 `src-tauri/`，目前很薄（仅 log、open 插件），可以通过 `tauri::command` 扩展
- 当前 CSP（`src-tauri/tauri.conf.json`）：`connect-src 'self' blob: ipc:`

### 需求

为扫描版（无文字层）PDF 增加 OCR 识别能力，使这类文件也能参与文本对比。

**已选定的技术路线：应用不内置 OCR，连接用户本机已安装好的 PaddleOCR 环境。** 用户已按官方方式安装：`uv add "paddleocr[doc-parser]"`（含表格识别等高级能力）。

### 具体要求

1. **环境检测与配置**

   - 应用启动时（或首次需要 OCR 时）自动检测本机可用的 PaddleOCR 环境：尝试常见的 uv venv 路径、`which python`/`python3` 并验证 `import paddleocr` 可用
   - 设置界面中允许用户手动指定 Python 解释器路径（或 uv 项目目录），并做有效性验证（实际跑一条 `python -c "import paddleocr; print(paddleocr.__version__)"`）
   - 检测不到环境时给出明确引导文案（如何安装），不要静默失败
2. **调用方式（已确定：常驻本地 HTTP 服务）**

   - 通过 Rust 端 `tauri::command` 拉起 Python 子进程，启动一个**常驻本地 HTTP 服务**封装 PaddleOCR（模型只加载一次，避免每次对比都冷启动数秒~十几秒）
   - 服务要求：端口随机分配（启动时探测空闲端口）、仅监听 127.0.0.1、应用退出时可靠回收子进程（含异常退出路径，避免残留孤儿进程）；服务异常崩溃后下次请求时自动重启
   - 接口约定：输入 PDF 文件路径 + 页码范围；输出结构化 JSON（每页的文本行及置信度，**保留版面顺序**）
   - OCR 在服务进程中执行，超时与取消机制必须有（大文件 OCR 可能数分钟），UI 上显示进度并允许取消（取消时终止服务侧当前任务）
3. **与对比流程的集成**

   - 加载 PDF 时先走现有 `extractText`，若提取结果为空/极少（判定为扫描件），在主区域3（原始文件显示区)显著提示"该 PDF 无文字层，是否启用 OCR 识别？"，用户确认后才启动 OCR，**不要默认自动跑**
   - OCR 结果作为该侧文本进入现有对比流程，与文字版 PDF 的行为完全一致
   - OCR 结果按文件缓存（文件内容 hash 为 key），同一文件二次打开直接读缓存
4. **暂不做**：生成带文字层的可搜索 PDF（可作为后续迭代，本次只产出对比用纯文本）；表格结构识别结果的可视化（doc-parser 能力先透传保留字段即可）

### 约束

- 应用本体打包体积不得因此功能明显增加（Python 环境完全由用户自备，不打包任何模型/解释器）
- 保持离线：OCR 全程本地，不允许引入任何网络请求
- 遵循项目 CLAUDE.md：最小改动、匹配现有代码风格、功能模块化（OCR 相关逻辑独立成新文件，不堆进 `pdfview.js`）

### 验收标准

- 未装 PaddleOCR 的机器：功能入口给出安装引导，应用其他功能不受影响
- 装有 PaddleOCR 的机器：打开一份扫描版 PDF，确认后 OCR 出的文本进入对比，差异标注正常；同一文件第二次打开秒开（走缓存）
- OCR 进行中可取消，取消后应用状态干净（无残留子进程）

---

## 提示词 2：文本差异语义识别（云端大模型 API）

> ⛔ **本提示词已作废，不要实施。**
> 原因：本项目已确定走 deepseek harness（dsh）插件的路线。dsh 本身就是"LLM + 工具调用循环"，
> 语义分析由 dsh 直接阅读报告中的差异统计与差异片段完成。**应用侧不再需要** API Key 配置界面、
> 厂商兼容、prompt 编排与结果渲染，也不需要调整 CSP。
> 以下内容仅作历史记录保留，实现提示词 4 / 提示词 5 时请忽略本节的"具体要求"。

### 项目背景

同提示词 1 的"项目背景"。补充：差异计算在前端 `src/compute.js` 完成（基于 `lib/diff.min.js`），产出结构化的行级差异（新增/删除/修改），UI 上有"折叠相同行""仅看差异"等过滤能力（`src/filterbar.js`）。

### 需求

在现有字符级差异的基础上，增加**语义级分析**：判断这批文字差异对文档上下文语义的影响程度，给出"影响大 / 中 / 小"的结论与简要理由。

**已选定的技术路线：调用云端大模型 API，用户自行配置厂商与 API Key。** 不引入本地模型。

### 具体要求

1. **配置**

   - 设置界面：API Base URL、API Key、模型名，默认兼容 OpenAI 格式的 `/chat/completions`（这样 OpenAI、DeepSeek、通义、Kimi 等主流厂商都能接）；Key 本地安全存储（Tauri 侧存系统 keychain 或至少不明文落盘到仓库目录），永不回传除用户所配厂商之外的任何地址
   - 提供"测试连接"按钮验证配置可用
2. **分析流程**

   - 输入：从现有差异结果中提取**差异片段 + 必要的上下文窗口**（差异前后各若干行），不是整篇文档无脑发送；差异很多时分批/汇总，控制 token 消耗
   - 输出（要求模型返回结构化 JSON，程序解析而非展示原始文本）：整体影响等级（大/中/小）、一句话结论、以及逐处差异的简要说明（可选，按 token 预算取舍）
   - 结果展示在差异区域顶部/侧边，与现有 UI 风格一致
3. **隐私与降级**

   - 首次使用该功能时明确提示：差异内容将发送至用户配置的第三方 API
   - 未配置 API、网络失败、额度不足时，应用其他功能完全不受影响，该功能单独给出错误提示
4. **CSP 调整**：`tauri.conf.json` 的 `connect-src` 需放开到允许用户配置的 API 域名（实现时评估：按用户配置动态注入，或放宽为 `https:` 并在文档中说明安全影响）

### 约束

- 不引入前端框架/重型 SDK，直接 `fetch` 调 OpenAI 兼容接口
- 除该功能外应用保持离线可用
- 遵循项目 CLAUDE.md：最小改动、LLM 调用与结果渲染逻辑独立成模块文件

### 验收标准

- 配置有效 Key 后：对一份有实质语义变化（如合同金额、责任条款改动）的对比，结论判"影响大"并给出理由；对仅措辞微调的对比，判"影响小"
- 未配置/断网时：功能入口提示明确，其余功能正常

---

## 提示词 3：批量对比结果导出（三种格式）

### 项目背景

同提示词 1 的"项目背景"。补充：

- 主区域 1（`#results`）：带差异颜色标注的对比结果视图（左右两栏，即"修整后文字"两侧）
- 主区域 3（`.pdf-panels`）：`带差异颜色标注的对比`原始文件渲染区（左右两个面板，PDF 为 canvas 渲染）
- 导出涉及的 4 个区块 = 区域 1 的左/右两栏 + 区域 3 的左/右两面板的当前视觉快照
- 批量对比为规划中的能力（见 worklist.md）：选定对比源文件夹后批量执行多对文件的对比。本功能服务于批量场景，但格式 ① 也适用于单次对比

### 需求

实现对比结果的导出，共两种产物：**① 单对自包含 HTML 报告；② 批量合并 HTML 报告（内含"左右分栏 / Redline 合并"双视图切换）**。**共同要求：自包含单文件，接收方零安装双击即看；包含带差异颜色标注的对比文字 + 原始文件的视觉快照（`带差异颜色标注的对比`，canvas 导出为图片内嵌）。**

#### 格式 ①：单对自包含 HTML 报告

- 一次对比导出为一个 .html：头部为摘要（文件名、对比时间、差异统计：新增/删除/修改各多少处），主体为 4 区块内容
- 保留只读交互：折叠/展开相同行、上一处/下一处差异跳转；所有 CSS/JS/图片内嵌，不依赖外部文件
- 本质是把当前页面的对比结果"冻结"为静态文件

#### 格式 ②③：批量合并 HTML 报告（分栏 / Redline 双视图切换，已确定合并为一个 HTML）

- 批量对比的全部结果放入**同一个** .html：
  - 开头是**总览**：每对文件一行（文件名、差异数、若提示词 3 已落地则附语义影响结论），每行是锚点超链接
  - 正文是各对文件的完整报告，按总览顺序排列，点击总览行跳转定位
- 每对文件的正文提供**两种视图，报告内一键切换**（数据同源，只是渲染不同）：
  - **左右分栏视图**：结构同格式 ①，带差异颜色标注的左右两栏 + 原始文件快照
  - **Redline 合并视图**：合并成一份文档，删除的内容用红色删除线、新增的内容用蓝色下划线，未变内容正常黑色——一份文件看完全部差异（法律行业 redline 风格）
  - 两种视图都离线可用（切换逻辑 JS 内嵌在报告里），记住用户上次选择的视图（报告内 localStorage 或默认分栏即可，不强制）
- 注意体积控制：原始文件快照图片按需压缩（降低分辨率/质量），并在批量数量大时给出导出耗时与体积预估提示

### 约束

- 纯前端实现：HTML 用字符串模板/DOM 序列化生成，图片用 canvas `toDataURL`；不引入服务端、不引入构建工具
- 模板与导出逻辑独立成模块文件（如 `src/report.js`），样式内联在模板中，与页面运行时样式解耦（避免页面样式改动破坏已导出报告）
- 遵循项目 CLAUDE.md：最小改动、不重构无关代码

### 验收标准

- 格式 ①：导出后在未安装本应用的电脑上双击打开，4 区块内容与颜色标注完整，折叠/差异跳转可用
- 格式 ②③：批量 10 对文件导出一个 HTML，总览 10 行、点击可跳转；Redline 视图增删颜色正确（红删除线/蓝下划线）、分栏与合并视图可离线切换
- **体积标准（已修订）**：快照清晰度优先，**不设单对体积上限**，不得为压体积而降低截图分辨率；改为按"一份报告最多 10 对"控制单份规模，超出由调用方分卷成多份报告（见提示词 4）

---

## 提示词 4：无头渲染通道（自动生成对比报告，供插件调用）

> 与提示词 3 的关系：**报告模板 `src/report.js` 两者共用**。提示词 3 是"人在页面里点导出"，本提示词是"无人工介入自动生成"。
> 建议先落地本提示词的第 1、2、3 项（管线解耦 + 无头通道），报告模板可先只做"分栏视图"跑通闭环，Redline 与批量总览随后补。

### 项目背景

同提示词 1 的"项目背景"。补充与本功能直接相关的事实：

- 主区域 1（`#results`）：带差异颜色标注的文字对比结果（左右两栏）
- 主区域 3（`.pdf-panels`）：原始文件渲染区（左/右两个面板），**两种格式的渲染方式不同**：
  - **PDF**：pdf.js 渲染进 `<canvas>`，**差异标注是叠在 canvas 之上的 DOM 层**（`.pdf-hl-layer` 内的 `.pdf-hl` 色块，见 `src/pdfview.js` 的 `addHlBox` / `paintInto`）。因此**直接取 canvas 拿不到标注**，`canvas.toDataURL()` 不可用
  - **docx**：docx-preview 的 `renderAsync` 渲染为 DOM（`.docx-host`，`breakPages: true` 真分页），差异标注是 DOM 元素（见 `src/docxview.js` 的 `applyHighlights` / `paintWrap`）。**docx 面板没有 canvas**
- 差异计算在 `src/compute.js`（纯逻辑、无 DOM，Node 下可直接 `require`，见 `test/smoke.js`）
- 已有双运行时抽象先例：`src/source-api.js`（浏览器 `/api/*` ↔ Tauri 命令）
- **仓库已有 headless Edge + CDP 驱动真实页面的成熟模式**：`test/e2e-pdf-fixes.js`、`test/e2e-scroll-docx.js`（启动 Edge 带 `--remote-debugging-port`、WebSocket 连 CDP、`Runtime.evaluate` 注入 JS 并取回值）。本功能直接复用该模式，**不要引入 Playwright / Puppeteer**

### 需求

让应用能在**无任何人工交互**的情况下完成一对或一批文件的对比，并产出**自包含 HTML 报告**（含带颜色标注的文字差异 + 原始文件的**视觉快照**），供 deepseek harness 插件调用（见提示词 5）。

**快照是硬需求**：提取出的文字不保证 100% 准确，原始文件的视觉呈现才是用户复核时的最终依据，因此报告必须包含原始文件"带差异颜色标注"的图像。

### 已选定的技术路线（不要偏离）

1. **快照采集用 CDP 的 `Page.captureScreenshot`（带 `clip`）直接截真实渲染结果。**
   它一次性把 canvas 与叠在其上的 DOM 标注层都拍进去，像素级等于用户在页面上所见。
   **禁止**引入 html2canvas、node-canvas、Playwright、Puppeteer 等替代方案。
2. **驱动用本机 Edge + CDP**，复用 `test/e2e-*.js` 已有模式。Edge 路径可通过 `EDGE_PATH` 环境变量覆盖，找不到时回落 Chrome，都找不到则给出明确错误。
3. **渲染复用现有页面代码**：新增无头入口页面，加载 `src/` 下同一套模块（`compute.js` / `pdfview.js` / `docxview.js`），**不重写任何渲染逻辑**。
4. **本阶段只做"浏览器 / CLI 模式"**（`node serve.js` 可用即成立），桌面（Tauri）模式留后续（见文末"后续扩展"）。

> **已有验证钉子可复用**：`tools/spike-capture.js` 已用真实页面跑通"启动 serve.js → headless Edge + CDP → 加载 PDF → 逐页截图（含差异标注）"全链路，
> 并沉淀了下面"已知的坑"一节的每一条。开发时**从这个脚本起步**（它已包含 CDP 连接、超时、看门狗、逐页滚动收敛等基础设施），
> 不要从零重写驱动层。运行：`node tools/spike-capture.js`（需要本机装有 Edge），产物在 `.spike-out/`（已加入 .gitignore）。

### 具体要求

#### 1. 管线解耦（前置重构）

- 把 `src/app.js` 中"加载一对文件 → 计算差异 → 把差异映射到结果区与 PDF/docx 面板并上色"这条逻辑抽成独立模块 `src/pipeline.js`
- 该模块**不依赖**标签页、历史记录、侧边栏与任何 UI 事件，输入输出均为普通数据（文件路径/字节 + 选项 → 差异结果 + 上色映射）
- `app.js` 改为调用 `pipeline.js`，**对外行为完全不变**，现有 `npm test` / `npm run test:all` 必须全绿

#### 2. 无头任务入口

- 新增 `tools/render.js`（Node CLI）：接受任务 JSON，启动临时静态服务（复用 `serve.js` 的 handler，参照 `test/api-test.js` 的用法）、拉起 headless Edge、驱动页面、写报告、退出
- 任务 JSON 至少包含：

  ```json
  {
    "title": "合同修订对比",
    "pairs": [{ "left": "D:\\a\\x.docx", "right": "D:\\b\\x.docx" }],
    "options": { "ignoreCase": true, "ignoreEol": true, "ignoreWhitespace": true,
                 "ignoreNewline": true, "ignoreWidth": true, "ignorePunct": true },
    "output": "D:\\reports\\out.html"
  }
  ```
- 标准输出返回结构化结果，退出码约定：成功 0、任务错误非 0

  ```json
  { "ok": true, "output": "...", "stats": { "pairs": 1, "added": 3, "removed": 1, "changed": 2 }, "warnings": [] }
  ```

#### 3. 页面内无头驱动

- 新增无头入口页面与驱动脚本，按任务依次完成每对文件的对比与面板渲染
- 截图前必须确保面板**处于可见状态**（不能被"隐藏编辑区"等 UI 状态影响），并等待：PDF 全部页 `render` 完成、docx `renderAsync` 完成、字体加载完成（`document.fonts.ready`）后才开始采集
- 单对文件失败不得中断整批：记入 `warnings` 并继续

#### 4. 快照采集

> 本节结论**已用 `tools/spike-capture.js` 实机验证**（8 页 PDF，headless Edge + CDP）。下面"已验证"的条目是实测结论，不要凭直觉推翻；"坑"一节的每一条都是踩过的，照做即可。

- 对主区域 3 的**左右两个面板分别截图**，**必须包含差异标注色块** ✅ 已验证：标注是叠在 canvas 之上的 DOM 层（`.pdf-hl`），`Page.captureScreenshot` 会连同 canvas 一起拍进去，像素级等于用户所见
- **必须以"页"为单位逐页截图**（PDF 取 `.pdf-page`、docx 取分页块），每页一张图，在报告中顺序排列 ✅ 已验证：逐页截图 8 页全部成功，每页内容完整、互不相同
- **不要试图"一次截完整篇"** ❌ 已验证不可靠：即使把面板 `height` 设为内容总高并 `overflow:visible`，实测一次截图仍只有前 2 页有内容、其余 6 页全空白（像素密度 23 字节/千像素，对比逐页截图的 86）。**逐页是唯一可靠路线**
- 逐页方案同时天然避开超长图触及浏览器纹理上限（约 16384px）的问题
- **分辨率不得为控制体积而刻意降低**：按 `scale=2` 采集，保证放大后文字清晰可辨（体积标准见提示词 3 的"已修订"条目）
- 固定 viewport 宽度与设备缩放，保证同一份文档多次输出结果一致

**实测数据（8 页 A4 PDF，单侧）**：`scale=1` 约 42KB/页；`scale=2` 约 202KB/页（1224×1584）。据此外推：10 页一对（左右）约 **3.9MB**，一份 10 对报告约 **39MB**。请把这个量级当作预期，不要当成异常。

##### 验证状态总览（2026-09-20，`tools/spike-capture.js` 实机跑出）

| 环节                        | 状态                 | 结论                                                                                                                                                                                    |
| --------------------------- | -------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 差异标注能否被 CDP 截进图里 | ✅ 已验证            | 能。标注是叠在 canvas 上的 DOM 层，`Page.captureScreenshot` 连 canvas 一起拍，像素级等于所见（对照实验：清空标注后同区域截图 md5 不同）                                               |
| 逐页滚动 + 逐页截图         | ✅ 已验证            | 8 页 PDF 全部成功，每页完整、互不相同                                                                                                                                                   |
| 一次截完整篇                | ❌ 已证伪            | 不可靠，只有前 2 页有内容。**逐页是唯一可靠路线**                                                                                                                                 |
| 长文档按页切的必要性        | ✅ 已验证            | 逐页同时天然避开 16384px 纹理上限                                                                                                                                                       |
| **docx 面板逐页截图** | ⚠️**未验证** | 夹具只做了 PDF。docx 是纯 DOM（无 canvas），机制上"滚动+逐页截"应同样成立，但**docx-preview 的"页"由 `breakPages` 生成，分页边界与跨页元素表现和 PDF 不同**，开发前应补一次验证 |
| 桌面（Tauri）模式           | ⚠️ 未验证          | 见文末"后续扩展"                                                                                                                                                                        |

##### 已知的坑（务必逐条处理）

1. **负/零高度的 clip 会让 `Page.captureScreenshot` 永久挂起**（不是报错，是静默卡死）。
   若目标页不在滚动视口内，`bot-top` 会算出负数，调用随即挂死。
   → 截图前必须校验 `clipH > 0`，且**给每个 CDP 调用加超时**，超时即失败而非卡住整批。
2. **截图前必须解除协同滚动绑定，并把重绑也掐死。**
   应用在布局变化时会调用 `SyncScroll.rebind(...)` 重新绑定。只调一次 `SyncScroll.rebind([])` 不够——实测部分页面的 `scrollTop` 会在 200ms 内被拉回 0，导致该页落在视口外。
   → 无头模式下：先 `SyncScroll.rebind([])`，再 `SyncScroll.rebind = function(){}`。
3. **`scrollTop` 要"写到稳定"，不能写一次就走。**
   → 循环：写 `scrollTop` → 等待 → 校验读回值 → 再观察一次确认没被拉回；不稳定就重试。
   → **末页的目标值会超过滚动上限**，必须 `Math.min(目标, scrollHeight - clientHeight)`，否则永远判定为"没收敛"，白白重试多轮。
4. **默认布局下 PDF 面板只有约 186px 高**，一页都放不下。
   原因：`.pdfarea` 有 `max-height: 65vh`，且被上方的主区域 1/2 挤压。
   → 无头模式注入样式：隐藏主区域 1/2 与两条拖拽条、`.pdfarea` 覆盖 `max-height:none` 并允许撑满，使面板高度足以容纳一整页（实测 1146px vs 页高 792px）。
5. **性能**：实测约 **815ms/页**（含滚动收敛等待）。100 页的文档约 80 秒。批量场景要显示进度，并考虑把等待时间调优（当前收敛等待偏保守）。
6. **协同滚动被解除后，PDF 面板与主区域 1 的行号锚定关系在截图期间不再维持**——这对截图无影响（我们只截面板），但说明无头模式下的面板状态与交互态不同，不要依赖界面上的滚动联动。

#### 5. 报告模板（`src/report.js`）

- **自包含单文件**：CSS / JS / 图片（base64 内嵌）全部内联，接收方零安装双击即看
- **单对报告**：头部摘要（文件名、对比时间、差异统计）+ 4 区块（区域 1 左/右 + 区域 3 左/右）
- **批量报告**：**一份报告最多 10 对**，超出由调用方分卷成多份（本模块不自行分卷）。开头为总览（每对一行：文件名、差异数、锚点超链接），正文按总览顺序排列
- 每对提供两种视图、报告内一键切换：**左右分栏** / **Redline 合并**（删除=红色删除线、新增=蓝色下划线）
- 保留只读交互：折叠/展开相同行、上一处/下一处差异跳转
- 模板与页面运行时样式**解耦**（页面改样式不得破坏已导出的报告）

#### 6. dsh 对接

见提示词 5。本提示词只负责把 `tools/render.js` 这个稳定入口做出来。

### 约束

- **不引入**前端框架，**不引入** html2canvas / node-canvas / Playwright / Puppeteer
- 无头渲染全程离线，**不产生任何网络请求**
- 排版优先复用浏览器原生的稳定性，不要自行实现截图后处理（拼接、切分）除非确有必要
- 遵循项目 CLAUDE.md：最小改动、匹配现有代码风格、新逻辑独立成文件（不堆进 `pdfview.js` / `docxview.js`）
- 现有测试必须全绿；新增能力要有对应的 Node 测试

### 验收标准

- `node tools/render.js --task demo.json` 在无任何人工介入下产出 `report.html`
- 报告在**未安装本应用**的电脑上双击打开：4 区块完整、差异颜色标注与页面所见一致、**快照放大后文字清晰可辨**
- 与页面手动对比同一对文件，报告中的颜色标注与页面**完全一致**
- PDF 与 docx 两种格式都能正确出快照；docx 分页块不漏页、不多页
- 全流程无网络请求；Edge/Chrome 均缺失时给出明确错误提示而非静默失败
- 一对文件失败时：其余对正常产出，`warnings` 中记录失败原因，退出码非 0

### 后续扩展（不在本次范围）

- **桌面（Tauri）模式**：exe 内没有 Node，`tools/render.js` 的驱动方式无法直接使用。两条路（按优先级）：
  1. **让 WebView2 开放调试端口**：在 `tauri.conf.json` 的 window 配置加
     `additionalBrowserArgs: "--remote-debugging-port=<端口> --disable-features=msWebOOUI,msPdfOOUI,msSmartScreenProtection"`
     （注意：wry 默认会传这三个 `--disable-features`，覆盖该字段时必须自行补回）。此后**插件可用与浏览器模式完全相同的 CDP 代码驱动桌面版**，成本极低。
     ⚠️ 调试端口**不得默认开启**（本机任意进程均可控制 webview），必须以启动参数或环境变量按需开启。
  2. app 内开隐藏窗口自行渲染，由 Rust 把结果写盘并回传（无需外部驱动，但改动量大）

---

## 提示词 5：deepseek harness（dsh）插件对接

### 项目背景

同提示词 4。落地本提示词的前提是：应用已能通过 `node tools/render.js --task task.json` 无头产出对比报告。

### 需求

让 dsh 能通过**自然语言对话**驱动本应用完成对比并产出报告：用户用一句话描述要比什么、怎么比，dsh 产出报告。

### 具体要求

1. **插件形态**：优先使用 harness 原生的**自定义命令 / skill**（一份描述文件 + 一次命令调用），**不引入常驻后台服务**。若 harness 支持 MCP，可再包一层极薄的 MCP server，内部复用同一个 CLI，不重复实现逻辑。
2. **自然语言 → 任务 JSON 由 harness 的 LLM 完成**。插件侧只提供**任务 JSON 的 schema 说明 + 若干范例**（写进 skill 描述文件），**不要自己写规则解析**（正则硬抽需求既脆弱又没必要，LLM 就是干这个的）。
3. 必须能被自然语言表达的需求：
   - 指定对比源文件夹 + 通配/名单，自动成对（原始=第一个 / 修改=第二个，或按文件名规则配对）
   - 六个忽略选项（大小写 / CRLF / 空格 / 换行 / 全半角 / 标点）
   - 输出路径与报告标题
4. **分卷**：一份报告最多 10 对。**超过 10 对时由插件自动切成多份报告**，并把全部报告路径返回给用户。
5. **调用协议**：插件只与 `tools/render.js` 的标准输出 / 退出码交互；错误必须结构化（JSON）且退出码非 0，便于 dsh 理解并向用户解释。
6. **语义分析不在应用内实现**（这是提示词 2 的替代）：报告产出后，由 dsh 直接阅读报告中的差异统计与差异片段做语义判断。**应用侧不配置任何大模型 API、不管理 API Key。**

### 约束

- 应用本体不得因此新增常驻进程或后台服务
- 插件层代码保持轻薄（目标 < 500 行），复杂逻辑一律留在应用侧
- 不得将报告内容或任务信息上传到任何网络地址

### 验收标准

- 对话说"把 `D:\a` 和 `D:\b` 下的合同按文件名两两对比，忽略大小写和空格，出一份报告"，dsh 能产出正确的任务 JSON 并成功调用，用户拿到可打开的报告
- 一次 25 对文件的需求：自动分成 3 份报告（10/10/5）并返回全部路径
- 未安装 Edge / 路径不存在 / 输入非法时：dsh 能拿到结构化错误并向用户说明原因，应用不崩溃
