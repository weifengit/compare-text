# 做一个类似“Diffchecker”的软件
结论：做核心文本对比不难，做完整类似 Diffchecker 的产品很难。
难度取决于你想做到什么程度。

目标：网页版文本 diff，粘贴两段文字对比

核心为什么不难？
因为 diff 算法和 UI 都有成熟方案：

算法库：jsdiff、diff-match-patch、Myers、Patience diff

编辑器：CodeMirror、Monaco

渲染：diff2html、react-diff-viewer

前端可用 Web Worker 防止大文本卡死

一个 MVP 只需要：行级对比 + 词级高亮 + 并排/内联视图 + 忽略大小写/空格 + 复制 unified diff。这些几天到几周就能做出来。

真正难的地方


Unicode 和边缘情况
换行符、编码、中文分词、CRLF/LF、BOM、超长行，都会影响结果。


产品体验
同步滚动、折叠相同行、词级高亮、历史记录

建议路线
先做最小可用版：

只支持粘贴文本对比；

纯前端计算，不上传服务器；

行级 + 词级高亮；

并排视图；

支持忽略大小写、空格（不仅包含收尾空格，还包含文字段落内部空格）、换行；
还支持一些全角与半角的识别，忽略差异（比如括号、冒号、引号、逗号、句号、分号、冒号、百分号、星号、加号、减号、等号、波浪号……）

用 jsdiff + CodeMirror + Web Worker。

node serve.js          # 启动后打开 http://localhost:3000


“   我是谁 我是%（）我是。   我是” VS  “  我是 谁
我是%()我是.我是”

全部忽略选项都勾选了，结论是“修改 1 · 新增 1 · 删除 0 行”


1、请在顶部添加一些功能区（后续可能会增加其他功能）：先添加一个功能“修整”，按下改功能之后，标注区域的【原，改】中的文本，会清除掉多余的空格、换行符、制表符、空行
2、标注区域：高度调整一下，缩小一半
3、“修改内容：增 3 · 删 6 字符”：这个部分请居中显示，并且显著一点，可以加大加粗字体

1、能不能实现回退，就是让这个“修整”功能可以撤销，回到之前的状态
2、并且我发现修整之后，文本放置区变成一行之后，不会在对应的区域内自动换行，而是出现了水平滚动条，体验不好，我希望在对应的区域内自动换行
3、我希望4个区域的边界清晰，并且上下对齐（标注区域应该也加上行标注区域，方便上下的对齐）。
4、我希望可以自行调整“标注区域”的高度（请添加对应的ui显示出高度可调整功能）

1、标注区域：也请实现行号显示区域
2、四个区域：边界感依然不明显，请使用颜色底纹区分，还有边框线条等
3、也添加一个垂直方向的拖动条，方便调整左右的宽度

“修整”功能撤销：现在是新增了一个按钮，我认为只要一个按钮即可，在切换之后就在原来的按钮上进行修改（同时改一下颜色）
行号显示区域：你似乎一直没理解，我是指类似现在“文本放置区域”左侧会显示1、2、3这样的行号，这个区域啊，请给“标注区域”的两个的左边也加上

存在两个问题：
1、标注区域的行号区域也应该覆盖到最下面，而不是截止在对应的行
2、修整为1行之后，标注区域也出现了水平滚动条不好看，也是希望这个区域可以自动换行

页面调整：
现在是将那些忽略的切换都放在了顶部【第一行：最顶部（左侧）、内联视图/并排视图……历史记录】【第二行：修整】，我希望修改为放在左侧，实现点击打开或隐藏的切换功能，窗口较小时，默认隐藏掉。
另外，请帮我思考一下还有那些能力可以加进来，让这个软件更加好用完善。


框架：
顶部第一行区域：功能按钮区
    - 显示/隐藏左侧边栏：现在按钮在右侧，请调整到左侧
    - 修整/取消修整:功能已实现
    - 清除：清空加入比较的内容，点击之后，标注区域、原始文字编辑区域均清空
    - 历史记录：功能已实现，但是保留的数量上限设置为100条
顶部第二行区域：对比源筛选区，包括【子文件夹的筛选（共用1套，显示“对比源”下的各个子文件夹名称，下拉选择其中一个子文件夹）、文件内部区域字段的筛选（共用1套，点击对应某个项目之后，会将对应项目的文本内容复制到主区域2）、文件的筛选（左右对比，应该有两套，控制筛选子文件夹下的文件，左边挑选原始文件，右边挑选修改文件）】
顶部第三行区域：对比差异信息展示区，已完成，显示类似”修改内容：增 8 · 删 8 字符”
主区域1-对比标注区域：已完成
主区域2-原始文字编辑区域：已完成，但需要在下方设置高度调节的按钮（方便控制原始文字编辑区域与“原始文件相关控件及文件显示区域”之间的距离），参考标注区域与文字编辑区域之间的调节方式
主区域3-原始文件相关控件及文件显示区域：先显示控件（自动缩放、适应宽度、适应页面、实际大小、全屏/还原显示（这个按钮按下之后，可以挤压掉顶部第三行区域、主区域1和2的显示，让其隐藏以释放更多显示空间或还原显示出来）），再下方显示对应的原始文件（PDF），分别显示“顶部第二行区域”筛选到的对应文件

左侧可隐藏/展开区域：
    - 对比源：设置原始文件的路径，获取路径下的全部文件，与“顶部第二行区域：对比源筛选区”形成关联关系
    - 对比设置：
        - 6个忽略


几个问题：
我希望左右两边的【原、改】区域可以协同滚动（注意根据内容进行所在区域），滚动时，需要带动主区域1、2、3也协同滚动
在第一行“功能按钮区”：1、添加“隐藏/显示”主区域2的功能，2、添加“折叠/展开相同行”功能


【自动缩放、适应宽度、适应页面、实际大小】：这几个按钮没有效果
现在的“跨区域协同滚动”实际体验比较差，有时候拖不动会卡住移动不了
【原始文件、修改文件】：点击筛选之后，没有立即响应，即选中了某个文件之后，应该立即渲染对应的PDF到对应的主区域1、2和3中

框架方面：
我希望把第一行“功能按钮区”中的【历史记录】直接放在左侧边栏中的“忽略选项”后面，并删除“视图与操作”部分的全部代码，再将【修整、清除、显示/隐藏编辑区、折叠/展开相同行】4个按钮调整到“顶部第二行区域”（右对齐）,"显示/隐藏左侧边栏"也放在“顶部第二行区域”（最左边），这样第一行就完全是空的（项目名“文本对比Text Diff · 离线纯前端”这个修改为“图标 文件对比”（右对齐）），第一行能不能实现tab的功能，即可以点击“+”之后，多开几个同样的对比的窗口进行对比其他内容


添加对比的“+”号，提现在最后一个对比的tab后面更好，美化一下tab标签，每个tab标签宽度也加宽一些，外观的话类似浏览器那种一格一格的方正的即可
主区域3-原始文件显示区域:这个部分的高度被限制了，我希望是不限制的，因为隐藏了编辑区、缩短了主区域1之后释放了大量空间被浪费，应该可以显示更多内容方便查看
主区域1没有实现左右协同滚动，也需要和主区域2、3协同滚动
主区域3：PDF预览，鼠标拖动时，不能选中其中的文字，另外，能否将对比差异的情况，直接提现并标注在PDF中，也类似区域1那样，对那些位置差异位置标注颜色背景
历史记录：请直接显示在左侧边栏中，不需要点击“历史记录”才显示出来，直接显示在左侧边栏（历史记录这些字后面，而不是显示在右上角）即可
在选中了子文件夹之后，原始文件和修改文件会默认填充好一些文件，此时在各个主区域中均没有实际渲染出来，导致我一定需要去修改对应的文件之后才能渲染，每个都是要重新选择，但我建议在你选中了子文件夹之后，默认填充了原始文件和修改文件之后，就直接根据这些内容渲染出主区域
忽略选项：默认全选中
