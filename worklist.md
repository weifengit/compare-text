# 做一个类似“Diffchecker”的软件
结论：做核心文本对比不难，做完整类似 Diffchecker 的产品很难。
难度取决于你想做到什么程度。

目标：网页版文本 diff，粘贴两段文字对比

核心为什么不难？
因为 diff 算法和 UI 都有成熟方案：

算法库：jsdiff、diff-match-patch、Myers、Patience diff

编辑器：CodeMirror、Monaco

渲染：diff2html、react-diff-viewer

前端可用 Web Worker 防止大文本卡死

一个 MVP 只需要：行级对比 + 词级高亮 + 并排/内联视图 + 忽略大小写/空格 + 复制 unified diff。这些几天到几周就能做出来。

真正难的地方


Unicode 和边缘情况
换行符、编码、中文分词、CRLF/LF、BOM、超长行，都会影响结果。


产品体验
同步滚动、折叠相同行、词级高亮、历史记录

建议路线
先做最小可用版：

只支持粘贴文本对比；

纯前端计算，不上传服务器；

行级 + 词级高亮；

并排视图；

支持忽略大小写、空格（不仅包含收尾空格，还包含文字段落内部空格）、换行；
还支持一些全角与半角的识别，忽略差异（比如括号、冒号、引号、逗号、句号、分号、冒号、百分号、星号、加号、减号、等号、波浪号……）

用 jsdiff + CodeMirror + Web Worker。

node serve.js          # 启动后打开 http://localhost:3000


“   我是谁 我是%（）我是。   我是” VS  “  我是 谁
我是%()我是.我是”

全部忽略选项都勾选了，结论是“修改 1 · 新增 1 · 删除 0 行”


1、请在顶部添加一些功能区（后续可能会增加其他功能）：先添加一个功能“修整”，按下改功能之后，标注区域的【原，改】中的文本，会清除掉多余的空格、换行符、制表符、空行
2、标注区域：高度调整一下，缩小一半
3、“修改内容：增 3 · 删 6 字符”：这个部分请居中显示，并且显著一点，可以加大加粗字体

1、能不能实现回退，就是让这个“修整”功能可以撤销，回到之前的状态
2、并且我发现修整之后，文本放置区变成一行之后，不会在对应的区域内自动换行，而是出现了水平滚动条，体验不好，我希望在对应的区域内自动换行
3、我希望4个区域的边界清晰，并且上下对齐（标注区域应该也加上行标注区域，方便上下的对齐）。
4、我希望可以自行调整“标注区域”的高度（请添加对应的ui显示出高度可调整功能）