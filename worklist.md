假设我要在这个项目中实现以下功能，你评估一下是否能实现，暂时只是评估，先不要写代码，评估实现后对打包之后的安装包大小，内存占用等方面的影响：

1. 添加PDF识别（（非文字版，所以需要ocr识别），使用环境包括paddleocr、表格识别等更高级的需求已单独安装 doc-parser 扩展：uv add "paddleocr[doc-parser]"），因为非文字版PDF不方便对比，需要生成可识别的PDF版本
3. 添加Word文档识别与打开呈现在主区域3中（如果没有好的方案，可以转化为PDF)
4. 添加文本差异语义识别，分析文字差异对文件上下文的影响大不大

做一个所有应用的底座：嵌入AI大模型聊天对话功能（需要自己配置大模型厂商APIkey)，后续在各个应用中均可以直接在这个底座上添加功能，将应用中的相关功能API暴露给聊天工具对应的大模型之后，大模型理解了响应的需求，方便通过对话的方式了解用户需求，最关键的是能够去完成一些批量的任务（可以上传文件），因为这个应用开发完成之后，通常已经可以实现单一任务，只是批量任务这种不太好去限定用户格式，刚好大模型是个缓冲或者说和事佬方便做桥接

最后使用tauri去打包成响应的应用

选定对比源之后，通过与AI对话的方式，了解其需求，获得批量对比的能力，分析出的需求可以保存方便后续再次使用，然后执行需求，生成对比图片【左、右、语义分析差异是否大的结论]，生成一个代办清单（人工核对)，这个代办清单中，可以挨个下载对比图片、也可以批量下载对比图片，还可以将对比导入对比区域回看、因为是任务，可以给这个打钩，打钩之后放入完成清单中，表示复核完成



我这个应用，我想实现对比之后页面中呈现的结果的导出（导出主区域1+主区域3共计4个区块的内容)，即包含有差异颜色标注的修整后的文字+原始文件（也是标注好颜色差异的PDF)，生成一个方便直接看出差异的“东西”，有什么比较好的方案，比如批量生成好这些“东西”之后，发给别人去肉眼分析对比，有什么好的格式吗，我暂时想到了“画布”或者说图片，将这4个区域直接贴在一个图片中，请搜索或告诉我一个优秀的大佬想到的比较好的方案，我可能比较狭隘。先不用写代码，告诉我方案即可


还是保持计划模式，请帮我写好提示词，方便我后续真正落地需要去开发这些功能时AI更清晰需求。

1、扫描版 PDF 的 OCR（PaddleOCR）：我倾向于用户自行装好环境，这个应用程序可以连接客户端中已经安装好的PaddleOCR环境进行识别
2、Word 文档识别与展示：docx-preview + mammoth.js 组合
3、文本差异语义识别：调用云端大模型API
4、批量对比结果导出方案：①自包含 HTML 报告（单文件）、②自包含 HTML 报告（批量对比中的全部对比差异放入同一个html中，在开头形成总览，不同对比文件形成跳转超链接方便定位）、③Redline（修订版多文档，删除的用红色删除线、新增的用蓝色下划线，一份文件看完所有差异，类似②）

我发现已经实现了可以直接显示docx文档在主区域3中，但是没有实现将差异情况（和主区域1保持一致）在docx文档的添加对应的标注（字体颜色红或者绿的），请帮我实现


docx 面板的协同滚动仍是比例同步，请修改为让 Word 面板也按行号锚定滚动。


如果我这个项目改成一个deepseek harness的插件，好改吗，工作量大不大

因为我这个项目也还在持续开发中，我后续想做的内容可以看/docs/feature-dev-prompts.md，总之，最终想实现的是，如果单独几个文件对比时，有一个类似现在项目中的UI页面直观看到区别，如果不想在UI上直接看差异的时候，或者文件非常多，我是想通过和dsh对话的方式告诉他怎么对比，然后让应用生成对比报告供我查看

读 docs/feature-dev-prompts.md 的「开发顺序与依赖」一节，
然后做 阶段 D：对接 dsh（提示词 5）
注意：提示词 2 已作废，不要实施。

报告存在一些问题：
1、【折叠相同行、上一处差异、下一处差异】这些按钮不要，没有用
2、主区域的左右两边，我希望在生成报告时，默认执行一遍“修整”变成单行，缩小空间占用
3、原始文件快照：我发现docx时，截图中存在一些问题，请看report-docx.html，第一页左侧截图把右侧的部分也截取进来了，然后左侧最后一行是“第 15 条 共享内容 alpha beta gamma delta“而第二页的第一行是”第 19 条 共享内容 alpha beta gamma delta"也就是有3条记录看不到没在图片中。

node tools/render.js --task demo.json && open demo/report-docx.html


请打开E:\code\compare-text\0056 沉香.html，请看对应的图片区域，存在的问题，截取的图片五颜六色的花纹，请看一下，修复。是不是滚动的问题，我说了生成报告的时候，可以不用左右两边按照字符或者段落去对齐，直接截图即可，比如左边3页右边4页，将每页直接转为图片（注意好是左边还是右边，并且注意顺序），然后将对应位置插入html中即可啊。左1右1、左2右2、左3右3、左（空）右4渲染出来即可。


# 1. 改版本号（三处保持一致）：
#    package.json / src-tauri/tauri.conf.json / src-tauri/Cargo.toml 的 version
# 2. 提交并推送 main
git add -A && git commit -m "提交信息" && git push origin main
# 3. 打 tag 并推送（tag 就是版本发布按钮）
git tag v0.3.0 && git push origin v0.3.0

