# 做一个类似“Diffchecker”的软件
结论：做核心文本对比不难，做完整类似 Diffchecker 的产品很难。
难度取决于你想做到什么程度。

目标：网页版文本 diff，粘贴两段文字对比

核心为什么不难？
因为 diff 算法和 UI 都有成熟方案：

算法库：jsdiff、diff-match-patch、Myers、Patience diff

编辑器：CodeMirror、Monaco

渲染：diff2html、react-diff-viewer

前端可用 Web Worker 防止大文本卡死

一个 MVP 只需要：行级对比 + 词级高亮 + 并排/内联视图 + 忽略大小写/空格 + 复制 unified diff。这些几天到几周就能做出来。

真正难的地方


Unicode 和边缘情况
换行符、编码、中文分词、CRLF/LF、BOM、超长行，都会影响结果。


产品体验
同步滚动、折叠相同行、词级高亮、历史记录

建议路线
先做最小可用版：

只支持粘贴文本对比；

纯前端计算，不上传服务器；

行级 + 词级高亮；

并排视图；

支持忽略大小写、空格（不仅包含收尾空格，还包含文字段落内部空格）、换行；
还支持一些全角与半角的识别，忽略差异（比如括号、冒号、引号、逗号、句号、分号、冒号、百分号、星号、加号、减号、等号、波浪号……）

用 jsdiff + CodeMirror + Web Worker。

node serve.js          # 启动后打开 http://localhost:3000


“   我是谁 我是%（）我是。   我是” VS  “  我是 谁
我是%()我是.我是”

全部忽略选项都勾选了，结论是“修改 1 · 新增 1 · 删除 0 行”


1、请在顶部添加一些功能区（后续可能会增加其他功能）：先添加一个功能“修整”，按下改功能之后，标注区域的【原，改】中的文本，会清除掉多余的空格、换行符、制表符、空行
2、标注区域：高度调整一下，缩小一半
3、“修改内容：增 3 · 删 6 字符”：这个部分请居中显示，并且显著一点，可以加大加粗字体

1、能不能实现回退，就是让这个“修整”功能可以撤销，回到之前的状态
2、并且我发现修整之后，文本放置区变成一行之后，不会在对应的区域内自动换行，而是出现了水平滚动条，体验不好，我希望在对应的区域内自动换行
3、我希望4个区域的边界清晰，并且上下对齐（标注区域应该也加上行标注区域，方便上下的对齐）。
4、我希望可以自行调整“标注区域”的高度（请添加对应的ui显示出高度可调整功能）

1、标注区域：也请实现行号显示区域
2、四个区域：边界感依然不明显，请使用颜色底纹区分，还有边框线条等
3、也添加一个垂直方向的拖动条，方便调整左右的宽度

“修整”功能撤销：现在是新增了一个按钮，我认为只要一个按钮即可，在切换之后就在原来的按钮上进行修改（同时改一下颜色）
行号显示区域：你似乎一直没理解，我是指类似现在“文本放置区域”左侧会显示1、2、3这样的行号，这个区域啊，请给“标注区域”的两个的左边也加上

存在两个问题：
1、标注区域的行号区域也应该覆盖到最下面，而不是截止在对应的行
2、修整为1行之后，标注区域也出现了水平滚动条不好看，也是希望这个区域可以自动换行

页面调整：
现在是将那些忽略的切换都放在了顶部【第一行：最顶部（左侧）、内联视图/并排视图……历史记录】【第二行：修整】，我希望修改为放在左侧，实现点击打开或隐藏的切换功能，窗口较小时，默认隐藏掉。
另外，请帮我思考一下还有那些能力可以加进来，让这个软件更加好用完善。


框架：
顶部第一行区域：功能按钮区
    - 显示/隐藏左侧边栏：现在按钮在右侧，请调整到左侧
    - 修整/取消修整:功能已实现
    - 清除：清空加入比较的内容，点击之后，标注区域、原始文字编辑区域均清空
    - 历史记录：功能已实现，但是保留的数量上限设置为100条
顶部第二行区域：对比源筛选区，包括【子文件夹的筛选（共用1套，显示“对比源”下的各个子文件夹名称，下拉选择其中一个子文件夹）、文件内部区域字段的筛选（共用1套，点击对应某个项目之后，会将对应项目的文本内容复制到主区域2）、文件的筛选（左右对比，应该有两套，控制筛选子文件夹下的文件，左边挑选原始文件，右边挑选修改文件）】
顶部第三行区域：对比差异信息展示区，已完成，显示类似”修改内容：增 8 · 删 8 字符”
主区域1-对比标注区域：已完成
主区域2-原始文字编辑区域：已完成，但需要在下方设置高度调节的按钮（方便控制原始文字编辑区域与“原始文件相关控件及文件显示区域”之间的距离），参考标注区域与文字编辑区域之间的调节方式
主区域3-原始文件相关控件及文件显示区域：先显示控件（自动缩放、适应宽度、适应页面、实际大小、全屏/还原显示（这个按钮按下之后，可以挤压掉顶部第三行区域、主区域1和2的显示，让其隐藏以释放更多显示空间或还原显示出来）），再下方显示对应的原始文件（PDF），分别显示“顶部第二行区域”筛选到的对应文件

左侧可隐藏/展开区域：
    - 对比源：设置原始文件的路径，获取路径下的全部文件，与“顶部第二行区域：对比源筛选区”形成关联关系
    - 对比设置：
        - 6个忽略


几个问题：
我希望左右两边的【原、改】区域可以协同滚动（注意根据内容进行所在区域），滚动时，需要带动主区域1、2、3也协同滚动
在第一行“功能按钮区”：1、添加“隐藏/显示”主区域2的功能，2、添加“折叠/展开相同行”功能


【自动缩放、适应宽度、适应页面、实际大小】：这几个按钮没有效果
现在的“跨区域协同滚动”实际体验比较差，有时候拖不动会卡住移动不了
【原始文件、修改文件】：点击筛选之后，没有立即响应，即选中了某个文件之后，应该立即渲染对应的PDF到对应的主区域1、2和3中

框架方面：
我希望把第一行“功能按钮区”中的【历史记录】直接放在左侧边栏中的“忽略选项”后面，并删除“视图与操作”部分的全部代码，再将【修整、清除、显示/隐藏编辑区、折叠/展开相同行】4个按钮调整到“顶部第二行区域”（右对齐）,"显示/隐藏左侧边栏"也放在“顶部第二行区域”（最左边），这样第一行就完全是空的（项目名“文本对比Text Diff · 离线纯前端”这个修改为“图标 文件对比”（右对齐）），第一行能不能实现tab的功能，即可以点击“+”之后，多开几个同样的对比的窗口进行对比其他内容


添加对比的“+”号，提现在最后一个对比的tab后面更好，美化一下tab标签，每个tab标签宽度也加宽一些，外观的话类似浏览器那种一格一格的方正的即可
主区域3-原始文件显示区域:这个部分的高度被限制了，我希望是不限制的，因为隐藏了编辑区、缩短了主区域1之后释放了大量空间被浪费，应该可以显示更多内容方便查看
主区域1没有实现左右协同滚动，也需要和主区域2、3协同滚动
主区域3：PDF预览，鼠标拖动时，不能选中其中的文字，另外，能否将对比差异的情况，直接提现并标注在PDF中，也类似区域1那样，对那些位置差异位置标注颜色背景
历史记录：请直接显示在左侧边栏中，不需要点击“历史记录”才显示出来，直接显示在左侧边栏（历史记录这些字后面，而不是显示在右上角）即可
在选中了子文件夹之后，原始文件和修改文件会默认填充好一些文件，此时在各个主区域中均没有实际渲染出来，导致我一定需要去修改对应的文件之后才能渲染，每个都是要重新选择，但我建议在你选中了子文件夹之后，默认填充了原始文件和修改文件之后，就直接根据这些内容渲染出主区域
忽略选项：默认全选中

1. Tab 栏美化 + ＋内联 — tabs.js:50 每次渲染把 ＋ 重挂到最后一个标签之后；styles.css 中 .tab 改为浏览器式方格（min-width:115px、白底 active 与下方相接、:nth 无下边框），.tab-add 与标签同高。

2. PDF 区高度不限制 — 根因 .pdf-panel{height:360px} 已删。.layout/.pdfarea/.pdf-panels/.pdf-panel 改为 flex 弹性链（grid-template-rows:minmax(0,1fr)），隐藏编辑区/缩短区域1后 PDF 区自动撑满。

3. 区域1 左右列协同滚动 — 根因是 renderFlow 的两个 .grid-body 无 id。已给 app.js renderFlow 加 id="leftBody"/"rightBody"（skipped 分支加 inline-body），currentScrollers() 即刻生效，与区域2/3 联动。

4. PDF 禁止选字 + 差异标注 — .pdf-panel{user-select:none}；pdfview.js 新增 collectItems（全局行计数器与 collectText 完全一致，保证行号对齐）、paintInto（vp.transform 换算文本基线坐标涂色）、applyHighlights、setHighlight/getHighlight/isLoaded；app.js updatePdfAnnotations 在每次渲染结果后把 diff 行喂给 PDF（grid 直接用结果行，flow 下按当前忽略选项补一次行级 diff，仅当 PDF 已加载）。.pdf-hl.rm/ad/ch 红/绿/琥珀背景。

5. 历史记录内联侧边栏 — 删除了"历史记录"按钮与右上角弹层，直接渲染到侧边栏 index.html:45 的 #historyList，随每次对比刷新；恢复/删除/清空逻辑保留。窄栏下时间缩写为 MM-DD HH:MM、摘要换行显示。

6. 选子文件夹立即渲染 — filterbar.js loadDir 默认成对填充（原始=第一个、修改=第二个文件）并新增 onDirChange（仅用户手动切换触发）；app.js onDirChange 加载当前 L/R 文件；loadSrcPath 也 .then(onDirChange)。顺手修了 loadFileToSide 令牌：改每侧 fileSeq 计数 + switchSeq 快照，L/R 并行加载不再互相作废，tab 切换仍可作废。

7. 忽略选项默认全选中 — 6 个复选框全部 checked（index.html），newTabState 六项全 true。

PDF的4种视图切换之后，渲染的PDF变成了空白的，看不到内容
协同滚动（包括左右与上下）会受到【显示编辑区、展开相同行、修整】按钮的影响，请修复


限制一下主区域3（原始PDF显示区）的高度，控制为界面高度的65%
对比源路径输入框：请修改为点击之后弹出选择路径的功能


132. 主区域3（原始PDF显示区）高度 — .pdfarea 加 max-height:65vh 限制为界面高度的65%；全屏（pdf-focus）模式 override 为不限高度，避免其余区域隐藏后下方留白浪费（styles.css）。
133. 对比源路径输入框 — 改为点击弹出文件夹选择弹层：serve.js 新增 /api/browse（列子目录+上一级、Windows 盘符枚举）；新增 src/picker.js（浏览/双击进入/上级/直达输入/确认回填）；输入框 readonly 仅用于展示已选路径，确认后走原 loadSrcPath 加载与渲染。
134. 文件夹选择弹层增强 — 路径输入框旁新增“加载”按钮（回车同效）：输入路径后点加载即浏览其子文件夹（停留在弹层内），点“选择此文件夹”才确认回填；另修复弹层用 hidden 属性却用 class 切换导致永不显示的 bug（open/close 改为操作 mask.hidden 属性，Edge 无头实测验证）。


在打开的浮动面板“选择文件夹”中，路径输入框输入内容之后，我希望有一个加载按钮，点击之后，加载输入框中的路径信息


我发现PDF显示的区域有几个问题：
1、显示的PDF总是模模糊糊的
2、PDF只有浏览查看功能不能用鼠标去选中其中的文字，能否改为可以选中
3、能否实现根据主区域1中分析出的差异，直接提现并标注在PDF中，也在类似区域1那样，对那些位置差异位置标注颜色背景

135. PDF 显示模糊 — canvas 按 devicePixelRatio 放大后备缓冲（backing=vp×dpr），渲染时叠加 [dpr,0,0,dpr,0,0] 变换再缩放回 CSS 尺寸；高分屏（dpr=2）下文字清晰锐利（src/pdfview.js，Edge 无头截图实测）。
136. PDF 文字可选中 — 新增透明文本覆盖层：每个 .pdf-page 叠加 .pdf-text-layer，按文本项变换矩阵（viewport.transform × item.transform 矩阵乘）逐项摆放 .pdf-tl 透明 div（top=基线-字身高度，不设 line-height 以贴合基线），浏览器原生拖选即得文本；同步移除 .pdf-panel 的 user-select:none。期间修复两处 vp.transform 当函数调用的 bug（其本质是矩阵数组），并给 collectItems 静默 catch 增加 __pdfCollectErr 留痕 + PdfView._debug 诊断钩子。
137. 差异标注到 PDF — 沿用并修正高亮层：collectItems 把文本项起始行号与 diff 行号映射（rm 红/ad 绿/ch 琥珀），paintInto 用同一矩阵乘法换算坐标，框高=字身×1.2 覆盖完整行；加载、setHighlight、视图切换/重排后均自动重绘。Edge 无头截图确认 ch/ad 条精确覆盖对应文本行。



138. PDF 差异标注精确到字符 + 修正垂直覆盖 — 变更行不再整行涂色：updatePdfAnnotations 直接复用 grid 行内字符片段（segL/segR，与区域1 高亮同源），左侧标 rm 片段、右侧标 ad 片段；pdfview 端 segRanges 算行内偏移区间，collectItems 给每文本项补 col（行内起始字符偏移）做区间∩项映射，x 用 canvas measureText 测量并按 PDF 实际项宽归一（测不出时按项宽均摊兜底）。垂直几何改为 [基线-字身, 基线+0.35×字身]，下伸笔画（g/y/p/q）完整覆盖。rm/ad 仍整行涂色。Edge 无头截图实测：'brown'/'Selection' 只框对应单词，下伸笔画在框内（src/pdfview.js、src/app.js）。

139. PDF 两处修复 — ① 拖选蓝色底纹只盖文字上半：.pdf-tl 行盒原为 1em 高被裁掉下伸区，改为盒高=字身×1.35 且 lineHeight=盒高，行盒撑满整盒（[基线-字身, 基线+0.35×字身]，与差异标注框同几何），底纹完整覆盖 g/y/p/q 下伸笔画（Edge 无头程序化选区截图实测）。② PDF 标注未遵循 6 个忽略选项：flow 模式（默认开忽略换行）下 updatePdfAnnotations 原先另算一份强制 ignoreNewline=false 的 grid diff，把换行/重排差异也标进 PDF；改为新增 putFlow 直接把 flow 结果的 segsL/segsR 按行切分为 {t:'rm'|'ad', segs} 标注映射——与区域1 完全同一数据流，6 个忽略按钮对 PDF 标注同步生效（src/pdfview.js、src/app.js；ui-init 新增回归用例，全套 26+25+11+3+5 绿）。



PDF滚动优化：左右两边可能存在页数不同的情况，还有文字大小布局不同，那么滚动的时候，可能造成对照不同步的情况，请思考一个正确的解决方案，让定位滚动更准确，方便3个主区域共6个区块滚动同步（文字内容的对应才是定位的关键）

140. 内容锚定协同滚动（替代比例同步） — 以“文本行号”为 6 个区块的公共坐标系：① 区域1 渲染行带 data-ls/data-le（grid 行=li/ri+1，折叠条=覆盖区间，flow 行=原行号）；② 编辑区用 CodeMirror coordsChar/heightAtLine 做 行号↔像素；③ pdfview 新增 lineOffset/lineAtOffset/lineHeight（基于 collectItems 文本项行号 + rect 法换算面板滚动像素，页数/字号/版式差异无关）。syncscroll 重写：setAdapter 注册各区块适配器，滚动时驱动区视口顶部 →「锚定行+行内偏移比例」→ 各区滚动到同一行；跨侧（原文↔修改）经 setTranslator 用 diff 对齐行(li,ri) 构表，线性插值+端点斜率1外推互译。无锚信息（flow 跨侧、PDF 未加载、桩环境）自动降级旧比例同步。新增“写入抑制”（程序化写 scrollTop 后 90ms 忽略该元素 scroll 事件）消除行号取整回声抖动；rerenderGridKeepScroll 改为按锚定行恢复（折叠增删行后比例已失真，行号不失真）。E2E 实测（test/e2e-scroll-anchor.js，headless Edge+CDP 驱动真实页面）：左 4 页/字号20/80 行 vs 右 3 页/字号14/85 行（插10删5），滚 PDF 左到行60 → PDF右=65、区域1左右=60/65、编辑区=60；滚区域1右列到行40 → 左列/PDF左/编辑区=30，7 断言全过；单元全套 11+5+3+26+11+1 绿。



历史记录：我看到限定了高度不合适，导致浪费下面的空间，改为合理的高度，并且我认为“清空全部”按钮应该放在界面可见区域的最底部，中间是历史记录记录，即动态控制中间的高度。
点击了“修整”按钮之后，PDF中的差异标注就失效了，请修复
我发现适应页面的时候，同一个PDF，前两页更小（宽度没有铺满页面），最后1页又变大显示（铺满了整个可用宽度，例如“E:\code\files\钟文峰-初审”下的炒谷芽-山西这个PDF）

141. 历史记录布局 — .sidebar 改 flex 纵列；.history-list 删掉 max-height:40vh，改 flex:1+overflow:auto 动态撑满侧边栏剩余高度（内部滚动条）；“清空全部”从列表 HTML 移出为 index.html 静态按钮 #historyClear 固定侧边栏最底部（无记录时 hidden），app.js renderHistory 只切换其显隐。因 .body-row 高度=主区域高度（PDF 区为底），按钮底边天然与 PDF 区底部对齐。
142. 修整后 PDF 标注失效 — 根因：标注行号坐标系=PDF 原文行号，修整把编辑器压成 1 行后 diff 行号全塌到第 1 行。修复：app.js 拆出 buildAnnotMaps，新增 syncPdfTextAnnotations——凡已加载 PDF 且 extractPanel 原文≠编辑器当前文本（修整/手工编辑），就用 PDF 原文+当前忽略选项另算一份 diff 覆盖该侧标注（与区域1 同一数据流，grid 行/flow 片段）；pdfview 的 extractPanel 增加原文缓存（getPanelText 同步可取），文本一致时零额外开销。E2E 实测：修整/撤销前后两侧标注映射逐字节一致。
143. 适应页面各页大小不一 — 根因：page 模式逐页异步渲染时各自算 scale，各页 MediaBox 不同/滚动条先后出现导致缩放不一致。修复：renderAll 重构为先 Promise.all 取齐全部页，computeScales 统一计算——page 模式取所有页约束的最严格 scale 统一应用（同 PDF 各页等大），width/auto/actual 同轮渲染共用一次量宽；renderPage 改为同步渲染（page 对象+scale 入参，单页失败跳过）。E2E 夹具末页 MediaBox 故意做窄，实测 4 页 scale 全等且=理论最严格值（test/e2e-pdf-fixes.js，9 断言全绿；全套单测 11+5+3+26+11+1 与滚动锚定 e2e 7 绿）。


对比的tab标签名称“对比1、对比2”等等不够清晰，请修改为真实的对象关键字（完全显示或精简显示两个对比的文件名、或者文本开头部分文字）
PDF显示模糊不清晰的问题，到底是为何，因为在WPS或者PDF阅读器中任何比例看都是清晰的，请修正（另外，我看到选中的文字后面有“影子”）
“字段(选择字段复制到编辑区)”：这个不需要在顶部显示了，因为又不能调整修改值，直接作为默认即可
添加两个功能：①左右互换（使用一个互换的按钮呈现在原、改之间，点击之后，调换左右文件），②添加“重置居中”按钮（“左右拖动调整列宽”之后，可能会左右两边的宽度不一样，我想点击这个按钮之后，实现左右两块区域是一样宽的）
历史记录动态控制中间的高度问题：页面的最大高度，请以主区域3（PDF）显示区域的底部为准，控制“清除全部”按钮的下方不能低于这个位置，中间显示历史记录较多时，使用内部的滚动条。

请在顶级标题行“请作者喝茶”超连接，点击去github项目的页面

“自动缩放”和“实际大小”似乎没有区别，那么删掉“实际大小”，另外，我希望“自动缩放”模式时，按住ctrl键+鼠标滚动可以控制放大或缩小比例（不是浏览器的显示比例），页面中显示缩放比例提示
修整：也需要清除字符串内部的空格
“新建对比窗口”：只要显示+号即可，去掉外周的虚线框